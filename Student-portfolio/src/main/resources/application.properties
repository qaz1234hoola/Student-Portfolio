# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/student_portfolio?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=harekrishnawithsrishti@12
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Settings
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Server Port
server.port=8080

# Logging Levels (Optional - helps with debugging)
logging.level.org.springframework.security=INFO
logging.level.org.hibernate.SQL=DEBUG

# JWT Secret (Must match the one in JwtTokenProvider if you hardcoded it there)
# If you used a variable in the code, define it here:
app.jwtSecret=YourSuperSecretKeyForJWTGenerationThatIsVeryLongAndSecure123
app.jwtExpirationInMs=86400000